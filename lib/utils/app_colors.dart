import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand (Warm & Trustworthy)
  static const Color primary = Color(0xFFE76F51); // Warm terracotta
  static const Color secondary = Color(0xFFF4A261); // Soft warm orange
  static const Color accent = Color(0xFFE9C46A); // Gentle highlight

  // Light Theme (Soft & Comfortable for Elder Eyes)
  static const Color lightBackground = Color(0xFFFAF7F2); // Warm beige
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightCard = Color(0xFFFDF3E7); // Soft cream card
  static const Color lightTextPrimary = Color(0xFF2D2A26);
  static const Color lightTextSecondary = Color(0xFF7A736B);

  // Dark Theme (Not pure black — eye friendly)
  static const Color darkBackground = Color(0xFF1F1B18); // Warm dark brown
  static const Color darkSurface = Color(0xFF2A2521);
  static const Color darkCard = Color(0xFF332D28);
  static const Color darkTextPrimary = Color(0xFFF5F1EB);
  static const Color darkTextSecondary = Color(0xFFCFC6BA);

  // Status Colors (Accessible)
  static const Color error = Color(0xFFD9534F); // Soft red (not harsh)
  static const Color success = Color(0xFF5C946E); // Calm green
  static const Color warning = Color(0xFFF4A261); // Matches theme
}