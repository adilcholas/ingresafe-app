import 'package:flutter/material.dart';

class AppColors {
  // Brand Primary (Your Choice)
  static const Color primary = Color(0xFF50C878); // Emerald Green

  // Risk Colors (Core Feature Logic)
  static const Color safe = Color(0xFF27AE60);
  static const Color caution = Color(0xFFF4C430);
  static const Color danger = Color(0xFFE74C3C);

  // Light Theme (Minimal Clean)
  static const Color lightBackground = Color(0xFFF7F9F8);
  static const Color lightSurface = Colors.white;
  static const Color lightTextPrimary = Color(0xFF0F172A);
  static const Color lightTextSecondary = Color(0xFF64748B);

  // Dark Theme (Modern Minimal)
  static const Color darkBackground = Color(0xFF0B0F0E);
  static const Color darkSurface = Color(0xFF111827);
  static const Color darkCard = Color(0xFF1F2937);
  static const Color darkTextPrimary = Colors.white;
  static const Color darkTextSecondary = Color(0xFF9CA3AF);
}
